---
description: "TEMPLATE — fill this in for YOUR project, then set alwaysApply: true. This file has the single highest impact of the entire pack."
alwaysApply: false
---

# Project Context (FILL ME IN)

<!--
WHY THIS FILE MATTERS MOST:
Generic rules fix behavior; project context fixes knowledge. The model can't guess
your folder conventions, your deploy quirks, or that `legacy/` is untouchable.
Delete these comments, fill in the sections, set alwaysApply: true above.
Keep it under ~100 lines — stale or bloated context is worse than none.
-->

## What this project is
<!-- 2-3 lines: product, users, current stage. e.g.
"B2B invoicing SaaS. Next.js 14 (App Router) + tRPC + Prisma + Postgres. ~40k LOC, in production with paying customers — regressions are expensive." -->

## Stack & versions
<!-- The exact truth, so the model stops guessing:
- Runtime: Node 20 / Python 3.12
- Framework: Next.js 14.2 (App Router — NO pages/ patterns)
- DB: Postgres 16 via Prisma; migrations with `pnpm prisma migrate dev`
- Package manager: pnpm ONLY
- Styling: Tailwind + shadcn/ui; tokens in tailwind.config.ts -->

## Commands
<!-- 
- Dev: `pnpm dev`
- Test: `pnpm vitest run` (single file: `pnpm vitest run path`)
- Typecheck: `pnpm tsc --noEmit`
- Lint: `pnpm lint`
- NEVER run: `pnpm db:reset` (wipes shared dev DB) -->

## Architecture map
<!-- Where things live and the golden path for a new feature:
- API: src/server/routers/* (tRPC) — one router per domain
- Business logic: src/server/services/* — routers stay thin
- UI: src/components/ui (shadcn, don't edit) vs src/components/app (ours)
- New feature = router + service + zod schema in src/schemas + component
- Copy an existing example: src/server/routers/invoice.ts is the reference implementation -->

## Conventions that differ from defaults
<!-- The stuff generic rules can't know:
- Errors: services throw AppError subclasses; routers map them — never raw Error
- Dates: always UTC in DB, formatted with src/lib/dates.ts (never raw Date methods)
- Feature flags via src/lib/flags.ts — new user-facing features go behind a flag
- Soft delete everywhere: use `deletedAt`, never DELETE rows -->

## Danger zones
<!-- 
- legacy/ — do not touch without asking; scheduled for removal
- src/server/billing/* — real money; changes need explicit approval + tests
- The `events` table has 200M rows — every query on it needs an index check -->

## Current priorities / known issues
<!-- Optional, keep fresh or delete:
- Migrating REST → tRPC; new endpoints must be tRPC
- Known flaky test: tests/e2e/checkout.spec.ts (issue #412) — rerun once before blaming your change -->
